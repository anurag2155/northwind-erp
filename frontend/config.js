// Deploy-time configuration. Set this to the live backend URL after the Render
// blueprint creates it; a ?api=<url> query parameter overrides it at runtime.
window.ERP_BASE = "http://127.0.0.1:8080";
